machine:
  timezone: Asia/Tokyo
  ruby:
    version: 2.1.3
  post:
    - sudo pip install awscli

test:
  pre:
    - chmod 764 bin/test.sh
    - ls -la bin
    - ./bin/test.sh
  override:
    - rspec spec/*.rb
